// Generated by CoffeeScript 1.11.1
module.exports = {
  getEntryFileName: function(entry, options) {
    var date, extension, i, len, name, parameter, ref;
    name = "";
    date = entry.date;
    if (date && (typeof options === "string")) {
      name = (date.format('YYYYMM')) + "_" + options + ".pdf";
    } else {
      if (date) {
        if (options.dateFormat != null) {
          name = date.format(options.dateFormat);
        } else {
          name = date.format('YYYYMM');
        }
      }
      name += "_" + options.vendor;
      if (options.others != null) {
        ref = options.others;
        for (i = 0, len = ref.length; i < len; i++) {
          parameter = ref[i];
          if (entry[parameter] != null) {
            name += "_" + entry[parameter];
          }
        }
      }
      extension = options.extension || 'pdf';
      name = name + "." + extension;
    }
    return name;
  }
};
